<?xml version="1.0"?>

<project name="DiscountStrategy" default="compile">
	OB
	<property name="classdir TEST" location="${blddir}/classes" />
				i

    <mkdir dir="${blddir}" />
    <mkdir dir="${classdir}" />
  </target>

  <target 
	  
	name="compile" depends="init">
    <javac
      srcdir="src"
      destdir="${classdir}" />
  </target>
</project>
